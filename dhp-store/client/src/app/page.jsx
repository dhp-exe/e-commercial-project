"use client";

/**
 * Temporary home page stub.
 * Points directly to the existing Home component during migration.
 * Will be updated in Step 3 to import from @/shared/pages/HomePage.
 */
import Home from '../pages/Home';

export default function Page() {
  return <Home />;
}

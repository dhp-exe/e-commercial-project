{
  "name": "dhp-store",
  "private": true,
  "description": "Full-stack deployment manager",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:all": "concurrently \"npm run dev:server\" \"npm run dev:client\" \"npm run dev:ai\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "dev:ai": "cd ai-service && (./venv/bin/uvicorn main:app --host 0.0.0.0 --port 10000 --reload || venv\\Scripts\\uvicorn main:app --host 0.0.0.0 --port 10000 --reload || uvicorn main:app --host 0.0.0.0 --port 10000 --reload)",
    "start:ai": "cd ai-service && (./venv/bin/uvicorn main:app --host 0.0.0.0 --port 10000 || venv\\Scripts\\uvicorn main:app --host 0.0.0.0 --port 10000 || python main.py)",
    "build": "npm install --prefix server",
    "start": "cd server && npm start"
  },
  "dependencies": {
    "concurrently": "^8.0.0",
    "helmet": "^8.1.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
